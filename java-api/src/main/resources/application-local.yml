spring:
  config:
    activate:
      on-profile: local
app:
  connection-strings:
    my-sql: "Server=localhost;Database=source_db;User=user;Password=pass;"
    postgres: "Host=localhost;Database=processed_db;Username=user;Password=pass;"
  minio:
    endpoint: "localhost:9000"
  kafka:
    bootstrap-servers: "localhost:9092"
  airflow:
    base-url: "http://localhost:8080/api/v1"